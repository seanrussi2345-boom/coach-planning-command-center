{
  "name": "coach-planning-command-center",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone football coaching planning prototype for real coaching staffs.",
  "scripts": {
    "serve": "python3 -m http.server 4173 --directory site",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20.9"
  }
}
